# JWUDTool GUI

A graphical front-end for Maschell’s JWUDTool.
The interface is built using Python 3 and Tkinter. It also uses wud2app.
# Feature List
Convert between WUX / WUD

Batch file processing

Decrypt WUD (requires keys)

Progress bar + live output

Auto-downloads JWUDTool.jar

Optional flags: -noVerify, -dev, -overwrite
